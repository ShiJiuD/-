<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dongdan.mapper.InquiryMapper">

    <!-- 插入询价 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO inquiry (inquiry_code, company_name, contact_name, phone, email,
                             product_category, quantity, description, status)
        VALUES (#{inquiryCode}, #{companyName}, #{contactName}, #{phone}, #{email},
                #{productCategory}, #{quantity}, #{description}, #{status})
    </insert>

    <!-- 管理端询价分页列表 -->
    <select id="pageAdmin" resultType="InquiryPageVO">
        SELECT id, inquiry_code,
        company_name, contact_name, phone, product_category,
        quantity,
        status,
        create_time
        FROM inquiry
        <where>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

</mapper>

